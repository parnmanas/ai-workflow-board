# Claude Code

The instructions for this repository are vendor-neutral and live in one place —
`AGENTS.md`, the cross-tool convention every agent here reads. This file exists
only so Claude Code picks them up too; it deliberately holds no content of its
own, because two copies drift and the drift is invisible until an agent acts on
the stale half.

@AGENTS.md
