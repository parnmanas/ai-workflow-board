{
  "name": "agent-manager",
  "version": "0.1.48",
  "private": true,
  "description": "Standalone subagent runner for AWB — drives Claude / Codex / Gemini CLIs without a host editor",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "awb-agent-manager": "dist/main.js"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('package.json','dist/package.json')\"",
    "start": "node dist/main.js",
    "test": "npm run build && node --test test/session-dedup.test.mjs && node --test test/moving-cue-resume.test.mjs && node --test test/gemini-adapter.test.mjs",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
